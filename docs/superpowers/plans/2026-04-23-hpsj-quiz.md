# Quiz Positionnement IA — HPSJ × Mister IA — Plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Quiz de positionnement en ligne pour classer les professionnels des hôpitaux Paris Saint-Joseph Marie-Lannelongue (Débutant / Intermédiaire / Avancé) pour les formations Microsoft Copilot Chat Mister IA.

**Architecture:** Next.js 15 App Router — RSC pour admin et page résultat, Client Components pour le quiz interactif et les filtres admin. Supabase PostgreSQL avec RLS et RPC `SECURITY DEFINER` pour la soumission atomique côté serveur. Le scoring est entièrement recalculé côté serveur — le client envoie seulement `{ identity, answers }`, jamais de score ou de level.

**Tech Stack:** Next.js 15 · TypeScript · Tailwind CSS · shadcn/ui · Supabase · react-hook-form · zod · jose (JWT HS256) · vitest · Vercel Analytics

**Spec de référence:** `docs/superpowers/specs/2026-04-23-hpsj-quiz-design.md` + `docs/superpowers/specs/2026-04-23-hpsj-quiz-corrections.md`

---

## Dépendances entre tâches

```
T1 (scaffold) → toutes
T2 (migration SQL) → T5 (clients), T13 (submit API), T19 (seed)
T3 (questions.ts) → T4 (scoring tests), T13 (submit API)
T4 (scoring.ts) → T13 (submit API)
T5 (clients + auth) → T9, T13, T15, T16, T17, T18
T6 (middleware) → T15 (admin login)
T7 (styles + header) → T8, T10, T11, T12, T14, T16, T17
T9 (check-email) → T10 (QuizForm)
T10 (QuizForm) → T12 (questions page, sessionStorage)
T11 (QuizQuestion + ProgressBar) → T12
T13 (submit API) → T12 (POST cible)
T14 (ResultCard) → T13 (redirect cible)
T15 (login) → T16 (dashboard)
T16 (dashboard) → T17, T18
T19 (seed) → après T2 + T13
T20 (deploy) → toutes
```

## Phases

| Phase | Tâches | Effort |
|-------|--------|--------|
| 1. Setup | T1–T2 | M |
| 2. Core lib | T3–T6 | M |
| 3. Styles + Landing | T7–T8 | S |
| 4. Quiz participant | T9–T14 | L |
| 5. Admin | T15–T18 | L |
| 6. Seed + Deploy | T19–T20 | S |

> ⚠️ **CHECKPOINT HUMAIN #1** — Après T2 : ouvrir Supabase Studio, vérifier que les tables `participants` et `responses` existent, que la RLS est activée, que la fonction `submit_quiz` est visible dans Database → Functions. Ne pas continuer sans ce check.

> ⚠️ **CHECKPOINT HUMAIN #2** — Après T19 : tester le quiz complet en local (landing → formulaire → 20 questions → résultat), vérifier les données dans Supabase Studio, tester l'admin (login → dashboard → export CSV). Valider avant déploiement production.

---

## Task 1 — Scaffold + Dépendances

**Effort :** M  
**Files :**
- Crée : projet Next.js dans le dossier courant
- Crée : `.env.local` (à partir de `.env.local.example`)
- Crée : `vitest.config.ts`
- Modifie : `package.json` (script test)

- [ ] **Étape 1 — Créer le projet Next.js 15**

```bash
npx create-next-app@latest . --typescript --tailwind --eslint --app --no-src-dir --import-alias "@/*"
```

Répondre : App Router = Yes, Turbopack = Yes (défaut).

- [ ] **Étape 2 — Installer les dépendances**

```bash
npm install @supabase/supabase-js jose react-hook-form @hookform/resolvers zod @vercel/analytics
npm install -D vitest @vitejs/plugin-react
```

- [ ] **Étape 3 — Initialiser shadcn/ui**

```bash
npx shadcn@latest init
```

Répondre : New York style, neutral color, CSS variables = Yes.

```bash
npx shadcn@latest add button input label checkbox select toast progress badge separator
npx shadcn@latest add sonner
```

- [ ] **Étape 4 — Créer `vitest.config.ts`**

```typescript
import { defineConfig } from 'vitest/config';
import path from 'path';

export default defineConfig({
  test: {
    environment: 'node',
  },
  resolve: {
    alias: {
      '@': path.resolve(__dirname, '.'),
    },
  },
});
```

- [ ] **Étape 5 — Ajouter le script test dans `package.json`**

Dans `package.json`, ajouter dans `"scripts"` :
```json
"test": "vitest run",
"test:watch": "vitest"
```

- [ ] **Étape 6 — Créer `.env.local.example`**

```bash
NEXT_PUBLIC_SUPABASE_URL=https://xxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGci...
SUPABASE_SERVICE_ROLE_KEY=eyJhbGci...
ADMIN_PASSWORD=motdepasse-admin-fort
ADMIN_SESSION_SECRET=chaine-aleatoire-32-chars-minimum
```

Copier en `.env.local` et remplir avec les vraies valeurs Supabase.

- [ ] **Étape 7 — Créer les dossiers nécessaires**

```bash
mkdir -p lib components supabase/migrations scripts public/logos __tests__
mkdir -p app/quiz/questions app/quiz/resultat
mkdir -p app/admin/login app/admin/dashboard "app/admin/participant/[id]"
mkdir -p app/api/submit-quiz app/api/check-email
mkdir -p app/api/admin/login app/api/admin/logout app/api/admin/export
```

- [ ] **Étape 8 — Commit**

```bash
git add package.json package-lock.json vitest.config.ts .env.local.example
git commit -m "chore: scaffold Next.js 15 + dépendances quiz HPSJ"
```

**Critère done :** `npm run dev` démarre sans erreur sur http://localhost:3000.

---

## Task 2 — Migration Supabase (SQL)

**Effort :** M  
**Files :**
- Crée : `supabase/migrations/001_init.sql`

- [ ] **Étape 1 — Créer la migration**

Créer `supabase/migrations/001_init.sql` avec le contenu suivant :

```sql
-- Table participants
CREATE TABLE participants (
  id           uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  first_name   text NOT NULL,
  last_name    text NOT NULL,
  email        text NOT NULL,
  service      text,
  started_at   timestamptz DEFAULT now(),
  completed_at timestamptz,
  total_score  int,
  max_score    int,
  level        text CHECK (level IN ('debutant', 'intermediaire', 'avance')),
  created_at   timestamptz DEFAULT now(),
  CONSTRAINT participants_email_unique UNIQUE (email)
);

CREATE INDEX idx_participants_email   ON participants(email);
CREATE INDEX idx_participants_level   ON participants(level);
CREATE INDEX idx_participants_created ON participants(created_at DESC);

-- Table responses
CREATE TABLE responses (
  id             uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  participant_id uuid NOT NULL REFERENCES participants(id) ON DELETE CASCADE,
  question_id    text NOT NULL,
  answer_values  jsonb NOT NULL,
  points_earned  int NOT NULL,
  created_at     timestamptz DEFAULT now()
);

CREATE INDEX idx_responses_participant ON responses(participant_id);

-- RLS : tout bloqué côté client, accès uniquement via RPC ou service_role
ALTER TABLE participants ENABLE ROW LEVEL SECURITY;
ALTER TABLE responses    ENABLE ROW LEVEL SECURITY;

-- Aucune policy = tout bloqué pour anon/authenticated (hors service_role)

-- RPC submit_quiz (atomique, SECURITY DEFINER)
CREATE OR REPLACE FUNCTION submit_quiz(
  p_first_name   text,
  p_last_name    text,
  p_email        text,
  p_service      text,
  p_completed_at timestamptz,
  p_total_score  int,
  p_max_score    int,
  p_level        text,
  p_responses    jsonb
) RETURNS uuid
LANGUAGE plpgsql SECURITY DEFINER AS $$
DECLARE v_id uuid;
BEGIN
  -- Validation
  IF p_email !~ '^[^@]+@[^@]+\.[^@]+$' THEN
    RAISE EXCEPTION 'email invalide';
  END IF;
  IF trim(p_first_name) = '' OR trim(p_last_name) = '' THEN
    RAISE EXCEPTION 'nom/prénom requis';
  END IF;
  IF p_level NOT IN ('debutant', 'intermediaire', 'avance') THEN
    RAISE EXCEPTION 'level invalide';
  END IF;
  IF jsonb_array_length(p_responses) = 0 THEN
    RAISE EXCEPTION 'responses vides';
  END IF;

  -- Normalisation
  p_email      := lower(trim(p_email));
  p_first_name := trim(p_first_name);
  p_last_name  := trim(p_last_name);

  -- UPSERT (conserve l'UUID si email déjà connu)
  INSERT INTO participants
    (first_name, last_name, email, service, completed_at, total_score, max_score, level)
  VALUES
    (p_first_name, p_last_name, p_email, p_service, p_completed_at, p_total_score, p_max_score, p_level)
  ON CONFLICT (email) DO UPDATE SET
    first_name   = EXCLUDED.first_name,
    last_name    = EXCLUDED.last_name,
    service      = EXCLUDED.service,
    completed_at = EXCLUDED.completed_at,
    total_score  = EXCLUDED.total_score,
    max_score    = EXCLUDED.max_score,
    level        = EXCLUDED.level
  RETURNING id INTO v_id;

  -- Supprime les anciennes réponses + réinsère
  DELETE FROM responses WHERE participant_id = v_id;

  INSERT INTO responses (participant_id, question_id, answer_values, points_earned)
  SELECT
    v_id,
    r->>'question_id',
    r->'answer_values',
    (r->>'points_earned')::int
  FROM jsonb_array_elements(p_responses) r;

  RETURN v_id;
END;
$$;

-- Permissions RPC
REVOKE EXECUTE ON FUNCTION submit_quiz FROM PUBLIC;
GRANT  EXECUTE ON FUNCTION submit_quiz TO anon, authenticated;
```

- [ ] **Étape 2 — Appliquer dans Supabase**

Dans Supabase Studio → SQL Editor, coller et exécuter le contenu de `001_init.sql`.

- [ ] **Étape 3 — ⚠️ CHECKPOINT HUMAIN #1**

Vérifier dans Supabase Studio :
- Tables `participants` et `responses` visibles dans Table Editor
- Database → Functions → `submit_quiz` visible
- Authentication → Policies : RLS activée sur les deux tables, aucune policy

- [ ] **Étape 4 — Commit**

```bash
git add supabase/migrations/001_init.sql
git commit -m "feat: migration SQL — tables participants/responses + RPC submit_quiz"
```

**Critère done :** Tables créées, RLS activée, RPC visible dans Supabase Studio.

---

## Task 3 — `lib/questions.ts`

**Effort :** M  
**Files :**
- Crée : `lib/questions.ts`

- [ ] **Étape 1 — Écrire `lib/questions.ts`**

```typescript
export type QuestionSection = 'profil' | 'prompting' | 'fonctionnalites' | 'usage-responsable';
export type QuestionType = 'single' | 'multiple';

export type QuestionOption = {
  value: string;
  label: string;
  points: number;
};

export type Question = {
  id: string;
  section: QuestionSection;
  type: QuestionType;
  label: string;
  helpText?: string;
  options: QuestionOption[];
  exclusiveValue?: string; // si sélectionné, désélectionne tout autre choix (et vice versa)
};

// Score max attendu : 79 pts (S1:30 + S2:17 + S3:19 + S4:13)
export const QUESTIONS: Question[] = [
  // ── Section 1 — Profil (max 30 pts) ──────────────────────────────────────
  {
    id: 'q1',
    section: 'profil',
    type: 'single',
    label: 'À quelle fréquence utilisez-vous Microsoft Copilot Chat dans votre travail ?',
    options: [
      { value: 'never',   label: 'Jamais',                          points: 0 },
      { value: 'tried',   label: "J'ai essayé une ou deux fois",    points: 1 },
      { value: 'monthly', label: 'Quelques fois par mois',          points: 3 },
      { value: 'daily',   label: 'Tous les jours ou presque',       points: 5 },
    ],
  },
  {
    id: 'q2',
    section: 'profil',
    type: 'multiple',
    label: 'Pour quelles tâches professionnelles avez-vous utilisé Copilot Chat ?',
    helpText: 'Plusieurs réponses possibles',
    exclusiveValue: 'none',
    options: [
      { value: 'none',     label: 'Aucun de ces usages',                         points: 0 },
      { value: 'emails',   label: 'Rédiger ou reformuler des emails',             points: 2 },
      { value: 'summaries',label: 'Faire des résumés de documents',               points: 2 },
      { value: 'research', label: 'Chercher des informations',                    points: 2 },
      { value: 'meetings', label: 'Préparer des réunions ou comptes-rendus',      points: 2 },
    ],
  },
  {
    id: 'q3',
    section: 'profil',
    type: 'single',
    label: 'Comment évaluez-vous votre niveau global avec les outils IA ?',
    options: [
      { value: 'novice',       label: "Je n'en ai jamais utilisé",                         points: 0 },
      { value: 'beginner',     label: "Je m'en sers occasionnellement",                     points: 2 },
      { value: 'intermediate', label: "Je m'en sers régulièrement et je suis à l'aise",    points: 3 },
      { value: 'advanced',     label: "Je les utilise intensivement et j'aide mes collègues", points: 5 },
    ],
  },
  {
    id: 'q4',
    section: 'profil',
    type: 'multiple',
    label: "En dehors de Copilot Chat, avez-vous utilisé d'autres outils IA ?",
    helpText: 'Plusieurs réponses possibles',
    options: [
      { value: 'none',       label: 'Aucun autre outil',       points: 0 },
      { value: 'chatgpt',    label: 'ChatGPT',                  points: 2 },
      { value: 'gemini',     label: 'Gemini (Google)',           points: 2 },
      { value: 'perplexity', label: 'Perplexity AI',            points: 2 },
    ],
  },
  {
    id: 'q5',
    section: 'profil',
    type: 'single',
    label: 'Avez-vous suivi une formation ou un accompagnement sur les outils IA ?',
    options: [
      { value: 'none',     label: 'Non, aucune formation',                          points: 0 },
      { value: 'auto',     label: 'Autodidacte (YouTube, articles, tutoriels)',     points: 2 },
      { value: 'internal', label: "Formation interne à l'établissement",            points: 4 },
      { value: 'external', label: 'Formation externe (organisme spécialisé)',       points: 6 },
    ],
  },

  // ── Section 2 — Prompting (max 17 pts) ───────────────────────────────────
  {
    id: 'q6',
    section: 'prompting',
    type: 'single',
    label: 'Comment formulez-vous généralement vos demandes à Copilot Chat ?',
    options: [
      { value: 'short',      label: 'En quelques mots simples',                              points: 1 },
      { value: 'sentence',   label: 'En une phrase claire',                                   points: 2 },
      { value: 'detailed',   label: 'Avec un contexte et des détails précis',                 points: 3 },
      { value: 'structured', label: 'Avec un rôle, un contexte, une tâche et un format attendu', points: 4 },
    ],
  },
  {
    id: 'q7',
    section: 'prompting',
    type: 'single',
    label: 'Donnez-vous un rôle à Copilot Chat (ex. « Tu es un expert en communication ») ?',
    options: [
      { value: 'never',     label: 'Non, jamais',             points: 0 },
      { value: 'sometimes', label: 'Parfois',                  points: 2 },
      { value: 'always',    label: 'Oui, systématiquement',   points: 3 },
    ],
  },
  {
    id: 'q8',
    section: 'prompting',
    type: 'multiple',
    label: 'Quels éléments incluez-vous habituellement dans vos prompts ?',
    helpText: 'Plusieurs réponses possibles',
    options: [
      { value: 'context',  label: 'Le contexte de la situation',                  points: 1 },
      { value: 'format',   label: 'Le format de réponse attendu (liste, tableau…)', points: 1 },
      { value: 'length',   label: 'La longueur souhaitée',                         points: 1 },
      { value: 'examples', label: 'Des exemples concrets',                         points: 1 },
    ],
  },
  {
    id: 'q9',
    section: 'prompting',
    type: 'single',
    label: 'Si la réponse de Copilot ne vous convient pas, que faites-vous ?',
    options: [
      { value: 'restart', label: "J'abandonne et recommence une nouvelle conversation",       points: 0 },
      { value: 'rephrase', label: 'Je reformule ma demande',                                  points: 2 },
      { value: 'iterate',  label: "Je précise ou affine ma demande dans la même conversation", points: 3 },
    ],
  },
  {
    id: 'q10',
    section: 'prompting',
    type: 'single',
    label: "Savez-vous ce qu'est le « prompt chaining » (enchaîner plusieurs prompts pour un résultat complexe) ?",
    options: [
      { value: 'no',    label: 'Non, je ne connais pas ce concept',               points: 0 },
      { value: 'heard', label: "J'en ai entendu parler mais je ne l'utilise pas", points: 1 },
      { value: 'use',   label: "Oui, je l'utilise régulièrement",                 points: 3 },
    ],
  },

  // ── Section 3 — Fonctionnalités (max 19 pts) ─────────────────────────────
  {
    id: 'q11',
    section: 'fonctionnalites',
    type: 'single',
    label: 'Avez-vous utilisé Copilot Chat dans Microsoft Teams (résumé de réunion, questions sur un chat…) ?',
    options: [
      { value: 'no',      label: 'Non, jamais',                                          points: 0 },
      { value: 'tried',   label: "J'ai essayé une fois",                                 points: 1 },
      { value: 'regular', label: 'Oui, régulièrement',                                   points: 2 },
      { value: 'expert',  label: 'Oui, et je combine plusieurs fonctions Teams + Copilot', points: 4 },
    ],
  },
  {
    id: 'q12',
    section: 'fonctionnalites',
    type: 'multiple',
    label: 'Parmi ces fonctionnalités de Copilot Chat, lesquelles avez-vous déjà utilisées ?',
    helpText: 'Plusieurs réponses possibles',
    options: [
      { value: 'web_search',        label: 'Recherche web intégrée',                   points: 1 },
      { value: 'document_analysis', label: 'Analyse de documents joints',               points: 1 },
      { value: 'code',              label: 'Aide à la rédaction ou correction de code', points: 1 },
      { value: 'translation',       label: 'Traduction de textes',                      points: 1 },
      { value: 'image_gen',         label: "Génération d'images",                       points: 1 },
    ],
  },
  {
    id: 'q13',
    section: 'fonctionnalites',
    type: 'single',
    label: "Avez-vous déjà généré des images avec Copilot ?",
    options: [
      { value: 'no',      label: "Non, je ne savais pas que c'était possible", points: 0 },
      { value: 'tried',   label: "J'ai essayé quelques fois",                  points: 1 },
      { value: 'regular', label: "Oui, je l'utilise régulièrement",            points: 3 },
    ],
  },
  {
    id: 'q14',
    section: 'fonctionnalites',
    type: 'single',
    label: 'Connaissez-vous Copilot Pages (espace de travail collaboratif avec l\'IA) ?',
    options: [
      { value: 'no',     label: "Non, jamais entendu parler",                          points: 0 },
      { value: 'heard',  label: "J'en ai entendu parler",                              points: 2 },
      { value: 'used',   label: "Je l'ai déjà utilisé",                                points: 3 },
      { value: 'expert', label: "Je l'utilise régulièrement pour collaborer",           points: 4 },
    ],
  },
  {
    id: 'q15',
    section: 'fonctionnalites',
    type: 'multiple',
    label: 'Quelles intégrations Microsoft 365 utilisez-vous avec Copilot ?',
    helpText: 'Plusieurs réponses possibles',
    options: [
      { value: 'none',    label: "Aucune pour l'instant",     points: 0 },
      { value: 'word',    label: 'Copilot dans Word',          points: 1 },
      { value: 'outlook', label: 'Copilot dans Outlook',       points: 1 },
      { value: 'excel',   label: 'Copilot dans Excel',         points: 1 },
    ],
  },

  // ── Section 4 — Usage responsable (max 13 pts) ───────────────────────────
  {
    id: 'q16',
    section: 'usage-responsable',
    type: 'single',
    label: 'Vérifiez-vous les informations fournies par Copilot Chat avant de les utiliser ?',
    options: [
      { value: 'never',     label: 'Non, je fais confiance aux réponses',                          points: 0 },
      { value: 'sometimes', label: 'Parfois, pour les informations importantes',                   points: 2 },
      { value: 'always',    label: "Oui, toujours — je vérifie avant d'utiliser",                  points: 3 },
    ],
  },
  {
    id: 'q17',
    section: 'usage-responsable',
    type: 'single',
    label: 'Partagez-vous des données patients ou des informations sensibles avec Copilot Chat ?',
    options: [
      { value: 'yes_often',     label: 'Oui, régulièrement',                               points: 0 },
      { value: 'yes_sometimes', label: 'Oui, parfois',                                      points: 1 },
      { value: 'no',            label: "Non, jamais — je sais que c'est interdit",           points: 3 },
    ],
  },
  {
    id: 'q18',
    section: 'usage-responsable',
    type: 'single',
    label: 'Connaissez-vous les principales limites de Copilot Chat (hallucinations, biais, données cutoff…) ?',
    options: [
      { value: 'no',      label: 'Non, je ne les connais pas',                    points: 0 },
      { value: 'partial', label: "J'en connais quelques-unes",                    points: 2 },
      { value: 'well',    label: "Je les connais bien et j'en tiens compte",      points: 3 },
    ],
  },
  {
    id: 'q19',
    section: 'usage-responsable',
    type: 'single',
    label: "Avez-vous conscience des obligations réglementaires liées à l'IA dans le secteur santé (RGPD, HDS…) ?",
    options: [
      { value: 'no',    label: "Non, ce n'est pas mon domaine",             points: 0 },
      { value: 'heard', label: "J'en ai entendu parler",                    points: 1 },
      { value: 'yes',   label: 'Oui, je connais les principales obligations', points: 2 },
    ],
  },
  {
    id: 'q20',
    section: 'usage-responsable',
    type: 'single',
    label: 'Comment percevez-vous l\'IA dans votre environnement de travail hospitalier ?',
    options: [
      { value: 'threat',      label: 'Comme une menace pour mon poste',                         points: 0 },
      { value: 'neutral',     label: "Je reste neutre, je l'utilise si nécessaire",              points: 1 },
      { value: 'opportunity', label: 'Comme un outil utile pour gagner en efficacité',           points: 2 },
    ],
  },
];
```

- [ ] **Étape 2 — Vérifier le score max manuellement**

S1: Q1(5)+Q2(8)+Q3(5)+Q4(6)+Q5(6) = 30  
S2: Q6(4)+Q7(3)+Q8(4)+Q9(3)+Q10(3) = 17  
S3: Q11(4)+Q12(5)+Q13(3)+Q14(4)+Q15(3) = 19  
S4: Q16(3)+Q17(3)+Q18(3)+Q19(2)+Q20(2) = 13  
**Total = 79** ✓

- [ ] **Étape 3 — Commit**

```bash
git add lib/questions.ts
git commit -m "feat: 20 questions quiz HPSJ avec types TypeScript"
```

**Critère done :** Fichier compilable sans erreur TypeScript (`npx tsc --noEmit`).

---

## Task 4 — `lib/scoring.ts` + Tests TDD

**Effort :** M  
**Files :**
- Crée : `lib/scoring.ts`
- Crée : `__tests__/scoring.test.ts`

- [ ] **Étape 1 — Écrire les tests d'abord (`__tests__/scoring.test.ts`)**

```typescript
import { describe, it, expect } from 'vitest';
import { calculateScore, calculateMaxScore, classify, applyKnockout } from '../lib/scoring';
import { QUESTIONS } from '../lib/questions';
import type { Question } from '../lib/questions';

const singleQ: Question[] = [{
  id: 'test', section: 'profil', type: 'single', label: 'Test',
  options: [
    { value: 'low',  label: 'Low',  points: 1 },
    { value: 'high', label: 'High', points: 5 },
  ],
}];

const multipleQ: Question[] = [{
  id: 'test', section: 'profil', type: 'multiple', label: 'Test',
  options: [
    { value: 'none', label: 'Aucun', points: 0 },
    { value: 'a',    label: 'A',     points: 2 },
    { value: 'b',    label: 'B',     points: 3 },
  ],
}];

describe('calculateMaxScore', () => {
  it('retourne 79 pour les 20 questions réelles', () => {
    expect(calculateMaxScore(QUESTIONS)).toBe(79);
  });

  it('single → max des options', () => {
    expect(calculateMaxScore(singleQ)).toBe(5);
  });

  it('multiple → somme des points positifs uniquement', () => {
    expect(calculateMaxScore(multipleQ)).toBe(5); // 2+3, ignore 0
  });
});

describe('calculateScore', () => {
  it('single → points de l\'option sélectionnée', () => {
    expect(calculateScore({ test: ['high'] }, singleQ)).toBe(5);
  });

  it('multiple → somme des options sélectionnées', () => {
    expect(calculateScore({ test: ['a', 'b'] }, multipleQ)).toBe(5);
  });

  it('aucune réponse → 0', () => {
    expect(calculateScore({}, singleQ)).toBe(0);
  });

  it('option inconnue → 0', () => {
    expect(calculateScore({ test: ['x'] }, singleQ)).toBe(0);
  });
});

describe('classify', () => {
  it('< 35% → debutant', () => {
    expect(classify(27, 79)).toBe('debutant'); // 34.2%
  });

  it('>= 35% et < 70% → intermediaire', () => {
    expect(classify(28, 79)).toBe('intermediaire'); // 35.4%
    expect(classify(55, 79)).toBe('intermediaire'); // 69.6%
  });

  it('>= 70% → avance', () => {
    expect(classify(56, 79)).toBe('avance'); // 70.9%
    expect(classify(79, 79)).toBe('avance'); // 100%
  });
});

describe('applyKnockout', () => {
  it('Q1=never ET Q2 inclut none → force debutant quel que soit le level', () => {
    expect(applyKnockout({ q1: ['never'], q2: ['none'] }, 'avance')).toBe('debutant');
    expect(applyKnockout({ q1: ['never'], q2: ['none'] }, 'intermediaire')).toBe('debutant');
  });

  it('Q1=never mais Q2 sans none → conserve le level', () => {
    expect(applyKnockout({ q1: ['never'], q2: ['emails'] }, 'intermediaire')).toBe('intermediaire');
  });

  it('Q2 inclut none mais Q1 != never → conserve le level', () => {
    expect(applyKnockout({ q1: ['daily'], q2: ['none'] }, 'avance')).toBe('avance');
  });

  it('pas de knockout → conserve le level', () => {
    expect(applyKnockout({ q1: ['daily'], q2: ['emails'] }, 'avance')).toBe('avance');
  });
});
```

- [ ] **Étape 2 — Vérifier que les tests échouent**

```bash
npm test
```

Attendu : FAIL (scoring.ts n'existe pas encore).

- [ ] **Étape 3 — Implémenter `lib/scoring.ts`**

```typescript
import type { Question } from './questions';

export type Level = 'debutant' | 'intermediaire' | 'avance';

export function calculateMaxScore(questions: Question[]): number {
  return questions.reduce((total, q) => {
    if (q.type === 'single') {
      return total + Math.max(...q.options.map(o => o.points));
    }
    return total + q.options.filter(o => o.points > 0).reduce((s, o) => s + o.points, 0);
  }, 0);
}

export function calculateScore(
  answers: Record<string, string[]>,
  questions: Question[]
): number {
  return questions.reduce((total, q) => {
    const selected = answers[q.id] ?? [];
    return total + q.options
      .filter(o => selected.includes(o.value))
      .reduce((s, o) => s + o.points, 0);
  }, 0);
}

export function classify(score: number, maxScore: number): Level {
  const pct = (score / maxScore) * 100;
  if (pct < 35) return 'debutant';
  if (pct < 70) return 'intermediaire';
  return 'avance';
}

export function applyKnockout(
  answers: Record<string, string[]>,
  level: Level
): Level {
  const neverUsed = answers['q1']?.[0] === 'never' && answers['q2']?.includes('none');
  return neverUsed ? 'debutant' : level;
}
```

- [ ] **Étape 4 — Vérifier que les tests passent**

```bash
npm test
```

Attendu : PASS (toutes les suites en vert).

- [ ] **Étape 5 — Commit**

```bash
git add lib/scoring.ts __tests__/scoring.test.ts
git commit -m "feat: scoring.ts (calculateScore, classify, knockout) + tests vitest"
```

**Critère done :** `npm test` retourne 0 erreur, 4 suites, 15+ tests en vert.

---

## Task 5 — Clients Supabase + `lib/auth.ts`

**Effort :** S  
**Files :**
- Crée : `lib/supabase-server.ts`
- Crée : `lib/supabase-client.ts`
- Crée : `lib/auth.ts`

- [ ] **Étape 1 — `lib/supabase-server.ts`**

```typescript
import 'server-only';
import { createClient } from '@supabase/supabase-js';

export function createSupabaseAdmin() {
  return createClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.SUPABASE_SERVICE_ROLE_KEY!
  );
}
```

- [ ] **Étape 2 — `lib/supabase-client.ts`**

```typescript
import { createClient } from '@supabase/supabase-js';

export const supabaseClient = createClient(
  process.env.NEXT_PUBLIC_SUPABASE_URL!,
  process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
);
```

- [ ] **Étape 3 — `lib/auth.ts`**

```typescript
import { SignJWT, jwtVerify } from 'jose';

function getSecret(): Uint8Array {
  const secret = process.env.ADMIN_SESSION_SECRET;
  if (!secret) throw new Error('ADMIN_SESSION_SECRET manquant');
  return new TextEncoder().encode(secret);
}

export async function signAdminJwt(): Promise<string> {
  return new SignJWT({})
    .setProtectedHeader({ alg: 'HS256' })
    .setIssuedAt()
    .setExpirationTime('24h')
    .sign(getSecret());
}

export async function verifyAdminSession(token: string): Promise<boolean> {
  try {
    await jwtVerify(token, getSecret());
    return true;
  } catch {
    return false;
  }
}
```

- [ ] **Étape 4 — Vérifier TypeScript**

```bash
npx tsc --noEmit
```

Attendu : aucune erreur.

- [ ] **Étape 5 — Commit**

```bash
git add lib/supabase-server.ts lib/supabase-client.ts lib/auth.ts
git commit -m "feat: clients Supabase + auth JWT admin"
```

---

## Task 6 — `middleware.ts`

**Effort :** S  
**Files :**
- Crée : `middleware.ts` (à la racine du projet)

- [ ] **Étape 1 — Créer `middleware.ts`**

```typescript
import { NextRequest, NextResponse } from 'next/server';
import { jwtVerify } from 'jose';

function getSecret(): Uint8Array {
  return new TextEncoder().encode(process.env.ADMIN_SESSION_SECRET!);
}

export async function middleware(request: NextRequest) {
  const { pathname } = request.nextUrl;

  if (!pathname.startsWith('/admin') || pathname.startsWith('/admin/login')) {
    return NextResponse.next();
  }

  const token = request.cookies.get('admin_session')?.value;
  if (!token) {
    return NextResponse.redirect(new URL('/admin/login', request.url));
  }

  try {
    await jwtVerify(token, getSecret());
    return NextResponse.next();
  } catch {
    return NextResponse.redirect(new URL('/admin/login', request.url));
  }
}

export const config = {
  matcher: ['/admin/:path*'],
};
```

- [ ] **Étape 2 — Commit**

```bash
git add middleware.ts
git commit -m "feat: middleware JWT — protection routes /admin/*"
```

**Critère done :** Naviguer vers `/admin/dashboard` redirige vers `/admin/login` (à tester après T15).

---

## Task 7 — Styles globaux + `components/Header.tsx` + logos

**Effort :** S  
**Files :**
- Modifie : `app/globals.css`
- Crée : `components/Header.tsx`
- Crée : `public/logos/mister-ia.svg`
- Crée : `public/logos/saint-joseph.svg`

- [ ] **Étape 1 — `app/globals.css` (variables CSS + base)**

Remplacer le contenu de `app/globals.css` par :

```css
@import "tailwindcss";

:root {
  --primary:      #2563EB;
  --primary-dark: #1E3A8A;
  --accent:       #F97316;
  --background:   #FFFFFF;
  --surface:      #F8FAFC;
  --text:         #0F172A;
  --text-muted:   #64748B;
  --success:      #22C55E;
  --error:        #EF4444;
}

body {
  background-color: var(--background);
  color: var(--text);
  font-family: 'Inter', system-ui, sans-serif;
}
```

- [ ] **Étape 2 — Ajouter Inter dans `app/layout.tsx`**

Modifier `app/layout.tsx` pour importer Inter via `next/font/google` :

```typescript
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import { Analytics } from '@vercel/analytics/react';
import { Toaster } from '@/components/ui/sonner';
import './globals.css';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Quiz Copilot Chat — Hôpitaux Paris Saint-Joseph',
  description: 'Évaluez votre niveau Microsoft Copilot Chat et trouvez la formation adaptée à vos besoins.',
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="fr">
      <body className={inter.className}>
        {children}
        <Toaster />
        <Analytics />
      </body>
    </html>
  );
}
```

- [ ] **Étape 3 — `components/Header.tsx`**

```typescript
import Image from 'next/image';
import Link from 'next/link';

export default function Header() {
  return (
    <header className="border-b border-gray-100 bg-white">
      <div className="max-w-4xl mx-auto px-4 py-4 flex items-center justify-between">
        <Link href="/" className="flex items-center gap-6">
          <Image
            src="/logos/mister-ia.svg"
            alt="Mister IA"
            width={120}
            height={40}
            priority
          />
          <span className="text-gray-300 text-xl font-light">×</span>
          <Image
            src="/logos/saint-joseph.svg"
            alt="Hôpitaux Paris Saint-Joseph Marie-Lannelongue"
            width={140}
            height={40}
            priority
          />
        </Link>
      </div>
    </header>
  );
}
```

- [ ] **Étape 4 — Logos SVG placeholders**

Créer `public/logos/mister-ia.svg` :

```svg
<svg xmlns="http://www.w3.org/2000/svg" width="120" height="40" viewBox="0 0 120 40">
  <rect width="120" height="40" fill="#1E3A8A" rx="4"/>
  <text x="10" y="26" font-family="Inter,sans-serif" font-size="14" font-weight="bold" fill="white">Mister IA</text>
</svg>
```

Créer `public/logos/saint-joseph.svg` :

```svg
<svg xmlns="http://www.w3.org/2000/svg" width="140" height="40" viewBox="0 0 140 40">
  <rect width="140" height="40" fill="#F8FAFC" rx="4"/>
  <text x="8" y="16" font-family="Inter,sans-serif" font-size="9" fill="#0F172A">Hôpitaux Paris</text>
  <text x="8" y="30" font-family="Inter,sans-serif" font-size="9" fill="#0F172A">Saint-Joseph</text>
</svg>
```

- [ ] **Étape 5 — Commit**

```bash
git add app/globals.css app/layout.tsx components/Header.tsx public/logos/
git commit -m "feat: styles globaux + header + logos placeholders"
```

---

## Task 8 — Landing page (`/`)

**Effort :** S  
**Files :**
- Modifie : `app/page.tsx`

- [ ] **Étape 1 — Écrire `app/page.tsx`**

```typescript
import Link from 'next/link';
import Header from '@/components/Header';
import { Button } from '@/components/ui/button';

export default function LandingPage() {
  return (
    <div className="min-h-screen flex flex-col">
      <Header />

      <main className="flex-1 flex items-center justify-center px-4 py-16">
        <div className="max-w-2xl w-full text-center space-y-8">
          <div className="space-y-4">
            <h1 className="text-4xl font-bold" style={{ color: 'var(--primary-dark)' }}>
              Quiz de positionnement<br />Microsoft Copilot Chat
            </h1>
            <p className="text-xl" style={{ color: 'var(--text-muted)' }}>
              Évaluez votre niveau en 5 à 10 minutes et découvrez le programme de formation adapté à votre profil.
            </p>
          </div>

          <div
            className="rounded-xl p-6 text-left text-sm"
            style={{ background: 'var(--surface)', color: 'var(--text-muted)' }}
          >
            <p className="font-semibold mb-2" style={{ color: 'var(--text)' }}>Informations sur vos données</p>
            <p>
              Vos réponses sont collectées uniquement dans le cadre de ce programme de formation.
              Elles ne seront pas partagées avec des tiers et seront conservées le temps nécessaire
              à l'organisation des sessions. Conformément au RGPD, vous pouvez demander l'accès,
              la rectification ou la suppression de vos données à{' '}
              <a href="mailto:formation@mister-ia.com" style={{ color: 'var(--primary)' }}>
                formation@mister-ia.com
              </a>.
            </p>
          </div>

          <Link href="/quiz">
            <Button
              size="lg"
              className="text-lg px-10 py-6"
              style={{ background: 'var(--primary)' }}
            >
              Commencer le quiz →
            </Button>
          </Link>

          <p className="text-sm" style={{ color: 'var(--text-muted)' }}>
            20 questions · 5 à 10 minutes · Résultat immédiat
          </p>
        </div>
      </main>

      <footer className="border-t border-gray-100 py-4 text-center text-sm" style={{ color: 'var(--text-muted)' }}>
        <p>© 2026 Mister IA · <a href="mailto:formation@mister-ia.com" style={{ color: 'var(--primary)' }}>Contact</a></p>
      </footer>
    </div>
  );
}
```

- [ ] **Étape 2 — Tester visuellement**

```bash
npm run dev
```

Ouvrir http://localhost:3000. Vérifier : header avec logos, titre, bloc RGPD, bouton CTA.

- [ ] **Étape 3 — Commit**

```bash
git add app/page.tsx
git commit -m "feat: landing page quiz HPSJ"
```

---

## Task 9 — `GET /api/check-email`

**Effort :** S  
**Files :**
- Crée : `app/api/check-email/route.ts`

- [ ] **Étape 1 — Créer `app/api/check-email/route.ts`**

```typescript
import { NextRequest, NextResponse } from 'next/server';
import { createSupabaseAdmin } from '@/lib/supabase-server';

export async function GET(request: NextRequest) {
  const email = request.nextUrl.searchParams.get('email');

  if (!email || !email.includes('@')) {
    return NextResponse.json({ exists: false });
  }

  const supabase = createSupabaseAdmin();
  const { data } = await supabase
    .from('participants')
    .select('id')
    .eq('email', email.toLowerCase().trim())
    .maybeSingle();

  return NextResponse.json({ exists: data !== null });
}
```

- [ ] **Étape 2 — Tester manuellement**

```bash
curl "http://localhost:3000/api/check-email?email=test@test.com"
```

Attendu : `{"exists":false}`

- [ ] **Étape 3 — Commit**

```bash
git add app/api/check-email/route.ts
git commit -m "feat: endpoint GET /api/check-email"
```

---

## Task 10 — `QuizForm.tsx` + page `/quiz`

**Effort :** M  
**Files :**
- Crée : `components/QuizForm.tsx`
- Crée : `app/quiz/page.tsx`

- [ ] **Étape 1 — Créer `components/QuizForm.tsx`**

```typescript
'use client';

import { useEffect, useRef, useState } from 'react';
import { useForm } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import { z } from 'zod';
import { useRouter } from 'next/navigation';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';
import { Checkbox } from '@/components/ui/checkbox';
import {
  Select, SelectContent, SelectItem, SelectTrigger, SelectValue,
} from '@/components/ui/select';

const schema = z.object({
  first_name: z.string().min(1, 'Prénom requis'),
  last_name:  z.string().min(1, 'Nom requis'),
  email:      z.string().email('Email invalide'),
  service:    z.string().optional(),
  rgpd:       z.literal(true, { errorMap: () => ({ message: 'Consentement requis' }) }),
});

type FormValues = z.infer<typeof schema>;

const SERVICES = [
  'Direction générale',
  'DSN',
  'RH',
  'Qualité',
  'Assistantes de direction',
  'Service international',
  'Autre',
];

export default function QuizForm() {
  const router = useRouter();
  const [emailWarning, setEmailWarning] = useState(false);
  const debounceRef = useRef<ReturnType<typeof setTimeout> | null>(null);

  const {
    register,
    handleSubmit,
    setValue,
    watch,
    formState: { errors, isSubmitting },
  } = useForm<FormValues>({ resolver: zodResolver(schema) });

  const emailValue = watch('email');

  useEffect(() => {
    if (!emailValue || !emailValue.includes('@')) {
      setEmailWarning(false);
      return;
    }
    if (debounceRef.current) clearTimeout(debounceRef.current);
    debounceRef.current = setTimeout(async () => {
      try {
        const res = await fetch(`/api/check-email?email=${encodeURIComponent(emailValue)}`);
        const { exists } = await res.json();
        setEmailWarning(exists);
      } catch {
        setEmailWarning(false);
      }
    }, 500);
    return () => { if (debounceRef.current) clearTimeout(debounceRef.current); };
  }, [emailValue]);

  async function onSubmit(data: FormValues) {
    const identity = {
      first_name: data.first_name,
      last_name:  data.last_name,
      email:      data.email,
      service:    data.service ?? null,
    };
    sessionStorage.setItem('quiz_identity', JSON.stringify(identity));
    router.push('/quiz/questions');
  }

  return (
    <form onSubmit={handleSubmit(onSubmit)} className="space-y-6">
      <div className="grid grid-cols-2 gap-4">
        <div className="space-y-1">
          <Label htmlFor="first_name">Prénom *</Label>
          <Input id="first_name" {...register('first_name')} />
          {errors.first_name && <p className="text-sm text-red-500">{errors.first_name.message}</p>}
        </div>
        <div className="space-y-1">
          <Label htmlFor="last_name">Nom *</Label>
          <Input id="last_name" {...register('last_name')} />
          {errors.last_name && <p className="text-sm text-red-500">{errors.last_name.message}</p>}
        </div>
      </div>

      <div className="space-y-1">
        <Label htmlFor="email">Email professionnel *</Label>
        <Input id="email" type="email" {...register('email')} />
        {errors.email && <p className="text-sm text-red-500">{errors.email.message}</p>}
        {emailWarning && (
          <p className="text-sm text-amber-600 bg-amber-50 border border-amber-200 rounded px-3 py-2">
            ⚠️ Un résultat existe déjà pour cet email. Si vous continuez, votre précédente passation sera remplacée par celle-ci.
          </p>
        )}
      </div>

      <div className="space-y-1">
        <Label htmlFor="service">Service (optionnel)</Label>
        <Select onValueChange={(v) => setValue('service', v)}>
          <SelectTrigger id="service">
            <SelectValue placeholder="Sélectionnez votre service" />
          </SelectTrigger>
          <SelectContent>
            {SERVICES.map(s => (
              <SelectItem key={s} value={s}>{s}</SelectItem>
            ))}
          </SelectContent>
        </Select>
      </div>

      <div className="flex items-start gap-3">
        <Checkbox
          id="rgpd"
          onCheckedChange={(checked) => setValue('rgpd', checked === true as true)}
        />
        <Label htmlFor="rgpd" className="text-sm leading-relaxed cursor-pointer">
          J'accepte que mes données soient utilisées dans le cadre de ce programme de formation
          conformément au RGPD. *
        </Label>
      </div>
      {errors.rgpd && <p className="text-sm text-red-500">{errors.rgpd.message}</p>}

      <Button
        type="submit"
        disabled={isSubmitting}
        className="w-full py-6 text-lg"
        style={{ background: 'var(--primary)' }}
      >
        {isSubmitting ? 'Chargement…' : 'Démarrer le quiz →'}
      </Button>
    </form>
  );
}
```

- [ ] **Étape 2 — Créer `app/quiz/page.tsx`**

```typescript
import Header from '@/components/Header';
import QuizForm from '@/components/QuizForm';

export default function QuizPage() {
  return (
    <div className="min-h-screen flex flex-col">
      <Header />
      <main className="flex-1 flex items-center justify-center px-4 py-12">
        <div className="max-w-lg w-full">
          <div className="mb-8 text-center">
            <h1 className="text-2xl font-bold mb-2" style={{ color: 'var(--primary-dark)' }}>
              Vos informations
            </h1>
            <p style={{ color: 'var(--text-muted)' }}>
              Ces informations permettent de vous orienter vers la formation adaptée.
            </p>
          </div>
          <div className="bg-white rounded-xl shadow-sm border border-gray-100 p-8">
            <QuizForm />
          </div>
        </div>
      </main>
    </div>
  );
}
```

- [ ] **Étape 3 — Tester visuellement**

Naviguer vers http://localhost:3000/quiz. Vérifier : formulaire, sélecteur service, checkbox RGPD, message avertissement email (tester avec un email déjà en base après le seed).

- [ ] **Étape 4 — Commit**

```bash
git add components/QuizForm.tsx app/quiz/page.tsx
git commit -m "feat: formulaire identité /quiz avec check-email debounced"
```

---

## Task 11 — `QuizQuestion.tsx` + `ProgressBar.tsx`

**Effort :** S  
**Files :**
- Crée : `components/QuizQuestion.tsx`
- Crée : `components/ProgressBar.tsx`

- [ ] **Étape 1 — Créer `components/ProgressBar.tsx`**

```typescript
type Props = { current: number; total: number };

export default function ProgressBar({ current, total }: Props) {
  const pct = Math.round((current / total) * 100);
  return (
    <div className="space-y-1">
      <div className="flex justify-between text-sm" style={{ color: 'var(--text-muted)' }}>
        <span>Question {current} / {total}</span>
        <span>{pct}%</span>
      </div>
      <div className="h-2 rounded-full" style={{ background: 'var(--surface)' }}>
        <div
          className="h-2 rounded-full transition-all duration-300"
          style={{ width: `${pct}%`, background: 'var(--primary)' }}
        />
      </div>
    </div>
  );
}
```

- [ ] **Étape 2 — Créer `components/QuizQuestion.tsx`**

```typescript
import type { Question } from '@/lib/questions';
import { Checkbox } from '@/components/ui/checkbox';
import { Label } from '@/components/ui/label';

type Props = {
  question: Question;
  selected: string[];
  onChange: (values: string[]) => void;
};

export default function QuizQuestion({ question, selected, onChange }: Props) {
  function handleSingle(value: string) {
    onChange([value]);
  }

  function handleMultiple(value: string) {
    const { exclusiveValue } = question;

    if (value === exclusiveValue) {
      // Sélectionner l'option exclusive → désélectionne tout le reste
      onChange([value]);
      return;
    }

    // Sélectionner une option normale → désélectionne l'option exclusive
    const without = selected.filter(v => v !== exclusiveValue);
    if (without.includes(value)) {
      onChange(without.filter(v => v !== value));
    } else {
      onChange([...without, value]);
    }
  }

  return (
    <div className="space-y-4">
      <div>
        <h2 className="text-xl font-semibold mb-1" style={{ color: 'var(--text)' }}>
          {question.label}
        </h2>
        {question.helpText && (
          <p className="text-sm" style={{ color: 'var(--text-muted)' }}>{question.helpText}</p>
        )}
      </div>

      <div className="space-y-3">
        {question.type === 'single' &&
          question.options.map(opt => (
            <label
              key={opt.value}
              className={`flex items-center gap-3 p-4 rounded-xl border-2 cursor-pointer transition-colors ${
                selected.includes(opt.value)
                  ? 'border-blue-500 bg-blue-50'
                  : 'border-gray-200 hover:border-gray-300'
              }`}
            >
              <input
                type="radio"
                name={question.id}
                value={opt.value}
                checked={selected.includes(opt.value)}
                onChange={() => handleSingle(opt.value)}
                className="sr-only"
              />
              <div className={`w-5 h-5 rounded-full border-2 flex items-center justify-center flex-shrink-0 ${
                selected.includes(opt.value) ? 'border-blue-500' : 'border-gray-400'
              }`}>
                {selected.includes(opt.value) && (
                  <div className="w-2.5 h-2.5 rounded-full bg-blue-500" />
                )}
              </div>
              <span className="text-sm">{opt.label}</span>
            </label>
          ))
        }

        {question.type === 'multiple' &&
          question.options.map(opt => (
            <label
              key={opt.value}
              className={`flex items-center gap-3 p-4 rounded-xl border-2 cursor-pointer transition-colors ${
                selected.includes(opt.value)
                  ? 'border-blue-500 bg-blue-50'
                  : 'border-gray-200 hover:border-gray-300'
              }`}
            >
              <Checkbox
                id={`${question.id}-${opt.value}`}
                checked={selected.includes(opt.value)}
                onCheckedChange={() => handleMultiple(opt.value)}
              />
              <Label htmlFor={`${question.id}-${opt.value}`} className="text-sm cursor-pointer">
                {opt.label}
              </Label>
            </label>
          ))
        }
      </div>
    </div>
  );
}
```

- [ ] **Étape 3 — Commit**

```bash
git add components/QuizQuestion.tsx components/ProgressBar.tsx
git commit -m "feat: composants QuizQuestion + ProgressBar"
```

---

## Task 12 — Page `/quiz/questions`

**Effort :** L  
**Files :**
- Crée : `app/quiz/questions/page.tsx`

- [ ] **Étape 1 — Créer `app/quiz/questions/page.tsx`**

```typescript
'use client';

import { useEffect, useReducer, useState } from 'react';
import { useRouter } from 'next/navigation';
import { toast } from 'sonner';
import { QUESTIONS } from '@/lib/questions';
import QuizQuestion from '@/components/QuizQuestion';
import ProgressBar from '@/components/ProgressBar';
import Header from '@/components/Header';
import { Button } from '@/components/ui/button';

type Identity = {
  first_name: string;
  last_name: string;
  email: string;
  service: string | null;
};

type State = {
  answers: Record<string, string[]>;
  currentIndex: number;
};

type Action =
  | { type: 'SET_ANSWER'; questionId: string; values: string[] }
  | { type: 'NEXT' }
  | { type: 'PREV' };

function reducer(state: State, action: Action): State {
  switch (action.type) {
    case 'SET_ANSWER':
      return {
        ...state,
        answers: { ...state.answers, [action.questionId]: action.values },
      };
    case 'NEXT':
      return { ...state, currentIndex: Math.min(state.currentIndex + 1, QUESTIONS.length - 1) };
    case 'PREV':
      return { ...state, currentIndex: Math.max(state.currentIndex - 1, 0) };
    default:
      return state;
  }
}

export default function QuestionsPage() {
  const router = useRouter();
  const [identity, setIdentity] = useState<Identity | null>(null);
  const [submitting, setSubmitting] = useState(false);
  const [state, dispatch] = useReducer(reducer, { answers: {}, currentIndex: 0 });

  useEffect(() => {
    const stored = sessionStorage.getItem('quiz_identity');
    if (!stored) {
      router.replace('/quiz');
      return;
    }
    setIdentity(JSON.parse(stored));
  }, [router]);

  useEffect(() => {
    window.scrollTo(0, 0);
  }, [state.currentIndex]);

  const currentQuestion = QUESTIONS[state.currentIndex];
  const currentAnswers = state.answers[currentQuestion.id] ?? [];
  const isLast = state.currentIndex === QUESTIONS.length - 1;
  const canContinue = currentAnswers.length > 0;

  async function handleSubmit() {
    if (!identity) return;
    setSubmitting(true);
    try {
      const res = await fetch('/api/submit-quiz', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ identity, answers: state.answers }),
      });

      if (!res.ok) {
        const { error } = await res.json().catch(() => ({ error: 'Erreur inconnue' }));
        throw new Error(error ?? 'Erreur serveur');
      }

      const { id } = await res.json();
      sessionStorage.removeItem('quiz_identity');
      router.push(`/quiz/resultat?id=${id}`);
    } catch (err) {
      setSubmitting(false);
      toast.error(err instanceof Error ? err.message : 'Une erreur est survenue. Veuillez réessayer.');
    }
  }

  if (!identity) return null;

  return (
    <div className="min-h-screen flex flex-col">
      <Header />
      <main className="flex-1 flex items-center justify-center px-4 py-8">
        <div className="max-w-2xl w-full space-y-8">
          <ProgressBar current={state.currentIndex + 1} total={QUESTIONS.length} />

          <div className="bg-white rounded-xl shadow-sm border border-gray-100 p-8">
            <QuizQuestion
              question={currentQuestion}
              selected={currentAnswers}
              onChange={(values) =>
                dispatch({ type: 'SET_ANSWER', questionId: currentQuestion.id, values })
              }
            />
          </div>

          <div className="flex justify-between gap-4">
            {state.currentIndex > 0 && (
              <Button
                variant="outline"
                onClick={() => dispatch({ type: 'PREV' })}
                disabled={submitting}
              >
                ← Précédent
              </Button>
            )}
            <div className="flex-1" />
            {!isLast && (
              <Button
                onClick={() => dispatch({ type: 'NEXT' })}
                disabled={!canContinue}
                style={{ background: canContinue ? 'var(--primary)' : undefined }}
              >
                Suivant →
              </Button>
            )}
            {isLast && (
              <Button
                onClick={handleSubmit}
                disabled={!canContinue || submitting}
                className="px-8 py-6 text-lg font-semibold"
                style={{ background: canContinue && !submitting ? 'var(--accent)' : undefined }}
              >
                {submitting ? 'Envoi en cours…' : 'Valider mes réponses ✓'}
              </Button>
            )}
          </div>
        </div>
      </main>
    </div>
  );
}
```

- [ ] **Étape 2 — Tester le parcours complet**

```
/ → /quiz → remplir formulaire → /quiz/questions → naviguer avec Suivant/Précédent → valider Q20
```

Vérifier : scroll vers le haut à chaque question, bouton désactivé sans réponse, Q20 a le style orange avec "Valider mes réponses".

**Note :** La soumission finale échouera jusqu'à ce que T13 (submit-quiz API) soit implémenté.

- [ ] **Étape 3 — Commit**

```bash
git add app/quiz/questions/page.tsx
git commit -m "feat: page quiz 20 questions avec useReducer + submit"
```

---

## Task 13 — `POST /api/submit-quiz`

**Effort :** M  
**Files :**
- Crée : `app/api/submit-quiz/route.ts`

- [ ] **Étape 1 — Créer `app/api/submit-quiz/route.ts`**

```typescript
import { NextRequest, NextResponse } from 'next/server';
import { z } from 'zod';
import { createSupabaseAdmin } from '@/lib/supabase-server';
import { QUESTIONS } from '@/lib/questions';
import { calculateScore, calculateMaxScore, classify, applyKnockout } from '@/lib/scoring';

const submitSchema = z.object({
  identity: z.object({
    first_name: z.string().min(1),
    last_name:  z.string().min(1),
    email:      z.string().email(),
    service:    z.string().nullable().optional(),
  }),
  answers: z.record(z.string(), z.array(z.string())),
});

export async function POST(request: NextRequest) {
  let body: unknown;
  try {
    body = await request.json();
  } catch {
    return NextResponse.json({ error: 'Payload invalide' }, { status: 400 });
  }

  // 1. Valider le payload (identity + answers bruts uniquement)
  const parsed = submitSchema.safeParse(body);
  if (!parsed.success) {
    return NextResponse.json({ error: 'Données invalides', details: parsed.error.flatten() }, { status: 400 });
  }

  const { identity, answers } = parsed.data;

  // 2. Recalculer le score côté serveur
  const total_score = calculateScore(answers, QUESTIONS);

  // 3. Recalculer le score max côté serveur
  const max_score = calculateMaxScore(QUESTIONS);

  // 4. Classifier côté serveur
  const rawLevel = classify(total_score, max_score);

  // 5. Appliquer la règle knockout côté serveur
  const level = applyKnockout(answers, rawLevel);

  // 6. Construire p_responses avec points_earned recalculés
  const p_responses = QUESTIONS.map(q => {
    const selected = answers[q.id] ?? [];
    const points_earned = q.options
      .filter(o => selected.includes(o.value))
      .reduce((s, o) => s + o.points, 0);
    return {
      question_id:   q.id,
      answer_values: selected,
      points_earned,
    };
  });

  // 7. Appel RPC Supabase avec toutes les valeurs calculées côté serveur
  const supabase = createSupabaseAdmin();
  const { data, error } = await supabase.rpc('submit_quiz', {
    p_first_name:   identity.first_name,
    p_last_name:    identity.last_name,
    p_email:        identity.email,
    p_service:      identity.service ?? null,
    p_completed_at: new Date().toISOString(),
    p_total_score:  total_score,
    p_max_score:    max_score,
    p_level:        level,
    p_responses:    p_responses,
  });

  if (error) {
    console.error('submit_quiz RPC error:', error);
    return NextResponse.json({ error: 'Erreur lors de l\'enregistrement' }, { status: 500 });
  }

  return NextResponse.json({ id: data });
}
```

- [ ] **Étape 2 — Tester le parcours complet**

Faire le quiz complet depuis `/quiz`. Vérifier dans Supabase Studio que le participant apparaît dans la table `participants` avec le bon niveau et score, et que les 20 réponses sont dans `responses`.

- [ ] **Étape 3 — Commit**

```bash
git add app/api/submit-quiz/route.ts
git commit -m "feat: POST /api/submit-quiz — scoring 100% serveur"
```

**Critère done :** Soumission quiz → participant visible en Supabase avec level/score corrects.

---

## Task 14 — `ResultCard.tsx` + page `/quiz/resultat`

**Effort :** S  
**Files :**
- Crée : `components/ResultCard.tsx`
- Crée : `app/quiz/resultat/page.tsx`

- [ ] **Étape 1 — Créer `components/ResultCard.tsx`**

```typescript
type Level = 'debutant' | 'intermediaire' | 'avance';

const LEVEL_CONFIG: Record<Level, {
  label: string;
  color: string;
  bgColor: string;
  message: string;
}> = {
  debutant: {
    label: 'Débutant',
    color: '#F97316',
    bgColor: '#FFF7ED',
    message: 'Vous êtes orienté(e) vers le programme Premiers pas avec Copilot Chat. Ce programme vous permettra de découvrir les fondamentaux de l\'IA au service de votre métier hospitalier.',
  },
  intermediaire: {
    label: 'Intermédiaire',
    color: '#2563EB',
    bgColor: '#EFF6FF',
    message: 'Vous êtes orienté(e) vers le programme Approfondir ses usages avec Copilot Chat. Ce programme vous aidera à maîtriser des techniques avancées et à gagner en productivité.',
  },
  avance: {
    label: 'Avancé',
    color: '#22C55E',
    bgColor: '#F0FDF4',
    message: 'Vous êtes orienté(e) vers le programme Exploiter Copilot au maximum. Ce programme vous permettra de devenir un référent IA dans votre service.',
  },
};

type Props = {
  level: Level;
  score: number;
  maxScore: number;
};

export default function ResultCard({ level, score, maxScore }: Props) {
  const config = LEVEL_CONFIG[level];
  const pct = Math.round((score / maxScore) * 100);

  return (
    <div className="space-y-6">
      <div
        className="rounded-2xl p-8 text-center"
        style={{ background: config.bgColor, border: `2px solid ${config.color}` }}
      >
        <p className="text-sm font-medium uppercase tracking-wide mb-2" style={{ color: config.color }}>
          Votre niveau
        </p>
        <h2 className="text-4xl font-bold mb-4" style={{ color: config.color }}>
          {config.label}
        </h2>
        <p className="text-base" style={{ color: 'var(--text)' }}>
          {config.message}
        </p>
      </div>

      <div className="text-center text-sm" style={{ color: 'var(--text-muted)' }}>
        Score : {score} / {maxScore} points ({pct}%)
      </div>

      <div className="rounded-xl p-6 text-center" style={{ background: 'var(--surface)' }}>
        <p className="text-sm" style={{ color: 'var(--text-muted)' }}>
          Vous recevrez prochainement les informations de formation par email.
          Pour toute question : <a href="mailto:formation@mister-ia.com" style={{ color: 'var(--primary)' }}>formation@mister-ia.com</a>
        </p>
      </div>
    </div>
  );
}
```

- [ ] **Étape 2 — Créer `app/quiz/resultat/page.tsx`**

```typescript
import { redirect } from 'next/navigation';
import Header from '@/components/Header';
import ResultCard from '@/components/ResultCard';
import { createSupabaseAdmin } from '@/lib/supabase-server';

type Props = { searchParams: Promise<{ id?: string }> };

export default async function ResultatPage({ searchParams }: Props) {
  const { id } = await searchParams;

  if (!id) redirect('/');

  const supabase = createSupabaseAdmin();
  const { data: participant } = await supabase
    .from('participants')
    .select('id, level, total_score, max_score, completed_at')
    .eq('id', id)
    .maybeSingle();

  if (!participant || !participant.completed_at) redirect('/');

  return (
    <div className="min-h-screen flex flex-col">
      <Header />
      <main className="flex-1 flex items-center justify-center px-4 py-12">
        <div className="max-w-lg w-full">
          <div className="text-center mb-8">
            <h1 className="text-2xl font-bold" style={{ color: 'var(--primary-dark)' }}>
              Votre résultat
            </h1>
          </div>
          <ResultCard
            level={participant.level as 'debutant' | 'intermediaire' | 'avance'}
            score={participant.total_score}
            maxScore={participant.max_score}
          />
        </div>
      </main>
    </div>
  );
}
```

- [ ] **Étape 3 — Tester**

Faire le quiz complet et vérifier que la page résultat affiche le bon niveau avec le bon message.

- [ ] **Étape 4 — Commit**

```bash
git add components/ResultCard.tsx app/quiz/resultat/page.tsx
git commit -m "feat: page résultat quiz + composant ResultCard"
```

---

## Task 15 — Admin login

**Effort :** S  
**Files :**
- Crée : `app/admin/login/page.tsx`
- Crée : `app/api/admin/login/route.ts`
- Crée : `app/api/admin/logout/route.ts`

- [ ] **Étape 1 — Créer `app/api/admin/login/route.ts`**

```typescript
import { NextRequest, NextResponse } from 'next/server';
import { signAdminJwt } from '@/lib/auth';

export async function POST(request: NextRequest) {
  const { password } = await request.json().catch(() => ({ password: '' }));

  if (password !== process.env.ADMIN_PASSWORD) {
    return NextResponse.json({ error: 'Mot de passe incorrect' }, { status: 401 });
  }

  const token = await signAdminJwt();

  const response = NextResponse.json({ ok: true });
  response.cookies.set('admin_session', token, {
    httpOnly: true,
    secure:   process.env.NODE_ENV === 'production',
    sameSite: 'strict',
    maxAge:   60 * 60 * 24, // 24h
    path:     '/',
  });
  return response;
}
```

- [ ] **Étape 2 — Créer `app/api/admin/logout/route.ts`**

```typescript
import { NextResponse } from 'next/server';

export async function POST() {
  const response = NextResponse.redirect(new URL('/admin/login', process.env.NEXT_PUBLIC_SITE_URL ?? 'http://localhost:3000'));
  response.cookies.delete('admin_session');
  return response;
}
```

- [ ] **Étape 3 — Créer `app/admin/login/page.tsx`**

```typescript
'use client';

import { useState } from 'react';
import { useRouter } from 'next/navigation';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';

export default function AdminLoginPage() {
  const router = useRouter();
  const [password, setPassword] = useState('');
  const [error, setError]       = useState('');
  const [loading, setLoading]   = useState(false);

  async function handleSubmit(e: React.FormEvent) {
    e.preventDefault();
    setError('');
    setLoading(true);
    try {
      const res = await fetch('/api/admin/login', {
        method:  'POST',
        headers: { 'Content-Type': 'application/json' },
        body:    JSON.stringify({ password }),
      });
      if (res.ok) {
        router.push('/admin/dashboard');
      } else {
        const { error: msg } = await res.json();
        setError(msg ?? 'Erreur de connexion');
      }
    } finally {
      setLoading(false);
    }
  }

  return (
    <div className="min-h-screen flex items-center justify-center px-4">
      <div className="max-w-sm w-full">
        <h1 className="text-2xl font-bold text-center mb-8" style={{ color: 'var(--primary-dark)' }}>
          Admin — Quiz HPSJ
        </h1>
        <form onSubmit={handleSubmit} className="bg-white rounded-xl shadow-sm border border-gray-100 p-8 space-y-4">
          <div className="space-y-1">
            <Label htmlFor="password">Mot de passe</Label>
            <Input
              id="password"
              type="password"
              value={password}
              onChange={e => setPassword(e.target.value)}
              autoFocus
            />
          </div>
          {error && <p className="text-sm text-red-500">{error}</p>}
          <Button
            type="submit"
            disabled={loading || !password}
            className="w-full"
            style={{ background: 'var(--primary)' }}
          >
            {loading ? 'Connexion…' : 'Se connecter'}
          </Button>
        </form>
      </div>
    </div>
  );
}
```

- [ ] **Étape 4 — Tester**

Naviguer vers `/admin/dashboard` → doit rediriger vers `/admin/login`. Se connecter avec le bon mot de passe → redirige vers `/admin/dashboard` (page 404 pour l'instant). Mauvais mot de passe → message d'erreur.

- [ ] **Étape 5 — Commit**

```bash
git add app/admin/login/page.tsx app/api/admin/login/route.ts app/api/admin/logout/route.ts
git commit -m "feat: admin login + cookie JWT + logout"
```

---

## Task 16 — Dashboard admin

**Effort :** L  
**Files :**
- Crée : `components/AdminFilters.tsx`
- Crée : `components/AdminTableHead.tsx`
- Crée : `components/AdminTable.tsx`
- Crée : `app/admin/dashboard/page.tsx`

- [ ] **Étape 1 — Créer `components/AdminFilters.tsx`**

```typescript
'use client';

import { useRouter, useSearchParams } from 'next/navigation';
import { useCallback } from 'react';
import { Input } from '@/components/ui/input';
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select';
import { Button } from '@/components/ui/button';

const SERVICES = ['Direction générale','DSN','RH','Qualité','Assistantes de direction','Service international','Autre'];

export default function AdminFilters() {
  const router = useRouter();
  const params = useSearchParams();

  const update = useCallback((key: string, value: string) => {
    const p = new URLSearchParams(params.toString());
    if (value && value !== 'all') {
      p.set(key, value);
    } else {
      p.delete(key);
    }
    p.delete('page');
    router.push(`/admin/dashboard?${p.toString()}`);
  }, [router, params]);

  return (
    <div className="flex flex-wrap gap-3 items-end">
      <Input
        placeholder="Rechercher nom, email…"
        defaultValue={params.get('q') ?? ''}
        onChange={e => update('q', e.target.value)}
        className="w-56"
      />

      <Select defaultValue={params.get('level') ?? 'all'} onValueChange={v => update('level', v)}>
        <SelectTrigger className="w-44"><SelectValue placeholder="Niveau" /></SelectTrigger>
        <SelectContent>
          <SelectItem value="all">Tous les niveaux</SelectItem>
          <SelectItem value="debutant">Débutant</SelectItem>
          <SelectItem value="intermediaire">Intermédiaire</SelectItem>
          <SelectItem value="avance">Avancé</SelectItem>
        </SelectContent>
      </Select>

      <Select defaultValue={params.get('service') ?? 'all'} onValueChange={v => update('service', v)}>
        <SelectTrigger className="w-52"><SelectValue placeholder="Service" /></SelectTrigger>
        <SelectContent>
          <SelectItem value="all">Tous les services</SelectItem>
          {SERVICES.map(s => <SelectItem key={s} value={s}>{s}</SelectItem>)}
        </SelectContent>
      </Select>

      <Select defaultValue={params.get('status') ?? 'complete'} onValueChange={v => update('status', v)}>
        <SelectTrigger className="w-40"><SelectValue placeholder="Statut" /></SelectTrigger>
        <SelectContent>
          <SelectItem value="complete">Complets</SelectItem>
          <SelectItem value="incomplete">Incomplets</SelectItem>
          <SelectItem value="all">Tous</SelectItem>
        </SelectContent>
      </Select>

      <div className="flex gap-2">
        <Input type="date" defaultValue={params.get('from') ?? ''} onChange={e => update('from', e.target.value)} className="w-36" />
        <Input type="date" defaultValue={params.get('to') ?? ''} onChange={e => update('to', e.target.value)} className="w-36" />
      </div>

      <Button variant="outline" onClick={() => router.push('/admin/dashboard')}>
        Réinitialiser
      </Button>

      <a href={`/api/admin/export?${params.toString()}`} download>
        <Button variant="outline">Export CSV</Button>
      </a>
    </div>
  );
}
```

- [ ] **Étape 2 — Créer `components/AdminTableHead.tsx`**

```typescript
'use client';

import { useRouter, useSearchParams } from 'next/navigation';

type Col = { key: string; label: string; sortable?: boolean };

const COLUMNS: Col[] = [
  { key: 'last_name',   label: 'Nom',      sortable: true },
  { key: 'first_name',  label: 'Prénom',   sortable: true },
  { key: 'email',       label: 'Email',    sortable: true },
  { key: 'service',     label: 'Service' },
  { key: 'created_at',  label: 'Date',     sortable: true },
  { key: 'total_score', label: 'Score',    sortable: true },
  { key: 'level',       label: 'Niveau',   sortable: true },
  { key: 'actions',     label: '' },
];

export default function AdminTableHead() {
  const router = useRouter();
  const params = useSearchParams();

  function handleSort(key: string) {
    const p = new URLSearchParams(params.toString());
    const currentSort = p.get('sort');
    const currentDir  = p.get('dir') ?? 'desc';
    p.set('sort', key);
    p.set('dir', currentSort === key && currentDir === 'desc' ? 'asc' : 'desc');
    p.delete('page');
    router.push(`/admin/dashboard?${p.toString()}`);
  }

  const sort = params.get('sort');
  const dir  = params.get('dir') ?? 'desc';

  return (
    <thead>
      <tr className="border-b border-gray-200">
        {COLUMNS.map(col => (
          <th key={col.key} className="px-4 py-3 text-left text-sm font-semibold" style={{ color: 'var(--text-muted)' }}>
            {col.sortable ? (
              <button
                onClick={() => handleSort(col.key)}
                className="flex items-center gap-1 hover:text-gray-900 transition-colors"
              >
                {col.label}
                {sort === col.key && (
                  <span className="text-xs">{dir === 'asc' ? '↑' : '↓'}</span>
                )}
              </button>
            ) : col.label}
          </th>
        ))}
      </tr>
    </thead>
  );
}
```

- [ ] **Étape 3 — Créer `components/AdminTable.tsx`**

```typescript
import Link from 'next/link';
import { Badge } from '@/components/ui/badge';

type Participant = {
  id:          string;
  first_name:  string;
  last_name:   string;
  email:       string;
  service:     string | null;
  created_at:  string;
  total_score: number | null;
  max_score:   number | null;
  level:       string | null;
  completed_at:string | null;
};

const LEVEL_LABEL: Record<string, { label: string; variant: 'default' | 'secondary' | 'destructive' | 'outline' }> = {
  debutant:      { label: 'Débutant',      variant: 'outline' },
  intermediaire: { label: 'Intermédiaire', variant: 'secondary' },
  avance:        { label: 'Avancé',        variant: 'default' },
};

type Props = { participants: Participant[] };

export default function AdminTable({ participants }: Props) {
  if (participants.length === 0) {
    return (
      <tbody>
        <tr>
          <td colSpan={8} className="px-4 py-12 text-center text-sm" style={{ color: 'var(--text-muted)' }}>
            Aucun participant trouvé.
          </td>
        </tr>
      </tbody>
    );
  }

  return (
    <tbody>
      {participants.map(p => {
        const isComplete = !!p.completed_at;
        return (
          <tr key={p.id} className="border-b border-gray-100 hover:bg-gray-50 transition-colors">
            <td className="px-4 py-3 text-sm font-medium">{p.last_name}</td>
            <td className="px-4 py-3 text-sm">{p.first_name}</td>
            <td className="px-4 py-3 text-sm" style={{ color: 'var(--text-muted)' }}>{p.email}</td>
            <td className="px-4 py-3 text-sm">{p.service ?? '—'}</td>
            <td className="px-4 py-3 text-sm" style={{ color: 'var(--text-muted)' }}>
              {new Date(p.created_at).toLocaleDateString('fr-FR')}
            </td>
            <td className="px-4 py-3 text-sm">
              {isComplete && p.total_score !== null
                ? `${p.total_score}/${p.max_score}`
                : '—'}
            </td>
            <td className="px-4 py-3 text-sm">
              {isComplete && p.level
                ? <Badge variant={LEVEL_LABEL[p.level]?.variant}>{LEVEL_LABEL[p.level]?.label ?? p.level}</Badge>
                : <span style={{ color: 'var(--text-muted)' }}>Incomplet</span>}
            </td>
            <td className="px-4 py-3 text-sm">
              {isComplete && (
                <Link
                  href={`/admin/participant/${p.id}`}
                  className="text-blue-600 hover:underline"
                >
                  Voir
                </Link>
              )}
            </td>
          </tr>
        );
      })}
    </tbody>
  );
}
```

- [ ] **Étape 4 — Créer `app/admin/dashboard/page.tsx`**

```typescript
import { Suspense } from 'react';
import Link from 'next/link';
import { createSupabaseAdmin } from '@/lib/supabase-server';
import AdminFilters from '@/components/AdminFilters';
import AdminTableHead from '@/components/AdminTableHead';
import AdminTable from '@/components/AdminTable';
import { Button } from '@/components/ui/button';

const PAGE_SIZE = 20;

type SearchParams = {
  level?:   string;
  service?: string;
  from?:    string;
  to?:      string;
  q?:       string;
  sort?:    string;
  dir?:     string;
  page?:    string;
  status?:  string;
};

type Props = { searchParams: Promise<SearchParams> };

export default async function DashboardPage({ searchParams }: Props) {
  const sp = await searchParams;
  const supabase = createSupabaseAdmin();

  const status  = sp.status ?? 'complete';
  const sort    = sp.sort   ?? 'created_at';
  const dir     = sp.dir    ?? 'desc';
  const page    = Math.max(1, parseInt(sp.page ?? '1', 10));
  const from    = (page - 1) * PAGE_SIZE;
  const to      = from + PAGE_SIZE - 1;

  // Stats en parallèle
  const [totalRes, levelRes, incompleteRes] = await Promise.all([
    supabase.from('participants').select('id', { count: 'exact', head: true }),
    supabase.from('participants').select('level').not('level', 'is', null),
    supabase.from('participants').select('id', { count: 'exact', head: true }).is('completed_at', null),
  ]);

  const totalCount      = totalRes.count ?? 0;
  const incompleteCount = incompleteRes.count ?? 0;
  const levelData       = levelRes.data ?? [];

  const levelCounts = { debutant: 0, intermediaire: 0, avance: 0 };
  for (const row of levelData) {
    if (row.level in levelCounts) levelCounts[row.level as keyof typeof levelCounts]++;
  }

  // Requête participants filtrée
  let query = supabase
    .from('participants')
    .select('id, first_name, last_name, email, service, created_at, total_score, max_score, level, completed_at', { count: 'exact' })
    .order(sort, { ascending: dir === 'asc' })
    .range(from, to);

  if (status === 'complete')   query = query.not('completed_at', 'is', null);
  if (status === 'incomplete') query = query.is('completed_at', null);
  if (sp.level)   query = query.eq('level', sp.level);
  if (sp.service) query = query.eq('service', sp.service);
  if (sp.from)    query = query.gte('created_at', sp.from);
  if (sp.to)      query = query.lte('created_at', sp.to + 'T23:59:59');
  if (sp.q) {
    const q = sp.q;
    query = query.or(`last_name.ilike.%${q}%,first_name.ilike.%${q}%,email.ilike.%${q}%`);
  }

  const { data: participants, count: filteredCount } = await query;
  const totalPages = Math.ceil((filteredCount ?? 0) / PAGE_SIZE);

  return (
    <div className="min-h-screen" style={{ background: 'var(--surface)' }}>
      <header className="bg-white border-b border-gray-100 px-6 py-4 flex items-center justify-between">
        <h1 className="text-xl font-bold" style={{ color: 'var(--primary-dark)' }}>
          Dashboard — Quiz HPSJ
        </h1>
        <form action="/api/admin/logout" method="POST">
          <Button type="submit" variant="outline" size="sm">Déconnexion</Button>
        </form>
      </header>

      <main className="max-w-7xl mx-auto px-6 py-8 space-y-8">
        {/* Stats cards */}
        <div className="grid grid-cols-3 gap-4">
          <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-100">
            <p className="text-sm mb-1" style={{ color: 'var(--text-muted)' }}>Total participants</p>
            <p className="text-3xl font-bold" style={{ color: 'var(--primary-dark)' }}>{totalCount}</p>
          </div>
          <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-100">
            <p className="text-sm mb-3" style={{ color: 'var(--text-muted)' }}>Répartition niveaux</p>
            <div className="space-y-1 text-sm">
              {(['debutant','intermediaire','avance'] as const).map(l => (
                <div key={l} className="flex justify-between">
                  <span className="capitalize">{l === 'intermediaire' ? 'Intermédiaire' : l === 'avance' ? 'Avancé' : 'Débutant'}</span>
                  <span className="font-medium">
                    {levelCounts[l]} ({totalCount > 0 ? Math.round(levelCounts[l] / totalCount * 100) : 0}%)
                  </span>
                </div>
              ))}
            </div>
          </div>
          <Link href="/admin/dashboard?status=incomplete" className="bg-white rounded-xl p-6 shadow-sm border border-gray-100 hover:border-amber-300 transition-colors">
            <p className="text-sm mb-1" style={{ color: 'var(--text-muted)' }}>Incomplets</p>
            <p className="text-3xl font-bold text-amber-500">{incompleteCount}</p>
          </Link>
        </div>

        {/* Filtres */}
        <Suspense>
          <AdminFilters />
        </Suspense>

        {/* Tableau */}
        <div className="bg-white rounded-xl shadow-sm border border-gray-100 overflow-hidden">
          <table className="w-full">
            <Suspense>
              <AdminTableHead />
            </Suspense>
            <AdminTable participants={participants ?? []} />
          </table>
        </div>

        {/* Pagination */}
        <div className="flex items-center justify-between text-sm" style={{ color: 'var(--text-muted)' }}>
          <span>Page {page} / {totalPages || 1} · {filteredCount ?? 0} participants</span>
          <div className="flex gap-2">
            {page > 1 && (
              <Link href={`/admin/dashboard?${new URLSearchParams({ ...sp, page: String(page - 1) })}`}>
                <Button variant="outline" size="sm">← Précédent</Button>
              </Link>
            )}
            {page < totalPages && (
              <Link href={`/admin/dashboard?${new URLSearchParams({ ...sp, page: String(page + 1) })}`}>
                <Button variant="outline" size="sm">Suivant →</Button>
              </Link>
            )}
          </div>
        </div>
      </main>
    </div>
  );
}
```

- [ ] **Étape 5 — Tester**

Se connecter à `/admin/login` → vérifier que le dashboard affiche les stats et le tableau. Tester les filtres, le tri par colonnes, la pagination.

- [ ] **Étape 6 — Commit**

```bash
git add components/AdminFilters.tsx components/AdminTableHead.tsx components/AdminTable.tsx app/admin/dashboard/page.tsx
git commit -m "feat: dashboard admin — filtres URL, tableau RSC, pagination"
```

---

## Task 17 — Détail participant `/admin/participant/[id]`

**Effort :** S  
**Files :**
- Crée : `app/admin/participant/[id]/page.tsx`

- [ ] **Étape 1 — Créer `app/admin/participant/[id]/page.tsx`**

```typescript
import { notFound } from 'next/navigation';
import Link from 'next/link';
import { createSupabaseAdmin } from '@/lib/supabase-server';
import { QUESTIONS } from '@/lib/questions';
import { Button } from '@/components/ui/button';

type Props = { params: Promise<{ id: string }> };

export default async function ParticipantDetailPage({ params }: Props) {
  const { id } = await params;
  const supabase = createSupabaseAdmin();

  const { data: participant } = await supabase
    .from('participants')
    .select('*')
    .eq('id', id)
    .maybeSingle();

  if (!participant) notFound();

  const { data: responses } = await supabase
    .from('responses')
    .select('question_id, answer_values, points_earned')
    .eq('participant_id', id);

  const responseMap = new Map((responses ?? []).map(r => [r.question_id, r]));

  const LEVEL_LABEL: Record<string, string> = {
    debutant: 'Débutant', intermediaire: 'Intermédiaire', avance: 'Avancé',
  };

  return (
    <div className="min-h-screen" style={{ background: 'var(--surface)' }}>
      <header className="bg-white border-b border-gray-100 px-6 py-4 flex items-center gap-4">
        <Link href="/admin/dashboard">
          <Button variant="outline" size="sm">← Retour</Button>
        </Link>
        <h1 className="text-xl font-bold" style={{ color: 'var(--primary-dark)' }}>
          {participant.first_name} {participant.last_name}
        </h1>
      </header>

      <main className="max-w-3xl mx-auto px-6 py-8 space-y-6">
        {/* Infos participant */}
        <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-100 grid grid-cols-2 gap-4 text-sm">
          <div><span className="font-medium">Email :</span> {participant.email}</div>
          <div><span className="font-medium">Service :</span> {participant.service ?? '—'}</div>
          <div><span className="font-medium">Niveau :</span> {LEVEL_LABEL[participant.level] ?? '—'}</div>
          <div><span className="font-medium">Score :</span> {participant.total_score ?? '—'}/{participant.max_score ?? '—'}</div>
          <div><span className="font-medium">Date :</span> {participant.created_at ? new Date(participant.created_at).toLocaleString('fr-FR') : '—'}</div>
        </div>

        {/* Réponses par question */}
        <div className="space-y-4">
          {QUESTIONS.map((q, i) => {
            const r = responseMap.get(q.id);
            const selected = (r?.answer_values ?? []) as string[];
            return (
              <div key={q.id} className="bg-white rounded-xl p-5 shadow-sm border border-gray-100">
                <p className="text-sm font-semibold mb-2" style={{ color: 'var(--text-muted)' }}>
                  Q{i + 1} — {q.section}
                </p>
                <p className="text-sm mb-3">{q.label}</p>
                <div className="space-y-1">
                  {q.options.map(opt => (
                    <div
                      key={opt.value}
                      className={`flex items-center justify-between px-3 py-2 rounded-lg text-sm ${
                        selected.includes(opt.value) ? 'bg-blue-50 border border-blue-200' : 'text-gray-400'
                      }`}
                    >
                      <span>{opt.label}</span>
                      {selected.includes(opt.value) && (
                        <span className="font-medium text-blue-700">+{opt.points} pts</span>
                      )}
                    </div>
                  ))}
                </div>
                {r && (
                  <p className="text-xs mt-2 text-right" style={{ color: 'var(--text-muted)' }}>
                    Points obtenus : {r.points_earned}
                  </p>
                )}
              </div>
            );
          })}
        </div>
      </main>
    </div>
  );
}
```

- [ ] **Étape 2 — Commit**

```bash
git add "app/admin/participant/[id]/page.tsx"
git commit -m "feat: page détail participant admin"
```

---

## Task 18 — Export CSV `/api/admin/export`

**Effort :** S  
**Files :**
- Crée : `app/api/admin/export/route.ts`

- [ ] **Étape 1 — Créer `app/api/admin/export/route.ts`**

```typescript
import { NextRequest, NextResponse } from 'next/server';
import { jwtVerify } from 'jose';
import { createSupabaseAdmin } from '@/lib/supabase-server';

function getSecret() {
  return new TextEncoder().encode(process.env.ADMIN_SESSION_SECRET!);
}

function escapeCsv(value: string | null | undefined): string {
  if (value == null) return '';
  const str = String(value);
  if (str.includes(';') || str.includes('"') || str.includes('\n')) {
    return `"${str.replace(/"/g, '""')}"`;
  }
  return str;
}

export async function GET(request: NextRequest) {
  const token = request.cookies.get('admin_session')?.value;
  if (!token) return new NextResponse('Non autorisé', { status: 401 });
  try { await jwtVerify(token, getSecret()); }
  catch { return new NextResponse('Session expirée', { status: 401 }); }

  const sp = request.nextUrl.searchParams;
  const supabase = createSupabaseAdmin();

  let query = supabase
    .from('participants')
    .select('id, first_name, last_name, email, service, created_at, total_score, max_score, level, completed_at')
    .order('created_at', { ascending: false });

  const status = sp.get('status') ?? 'complete';
  if (status === 'complete')   query = query.not('completed_at', 'is', null);
  if (status === 'incomplete') query = query.is('completed_at', null);
  if (sp.get('level'))   query = query.eq('level', sp.get('level')!);
  if (sp.get('service')) query = query.eq('service', sp.get('service')!);
  if (sp.get('from'))    query = query.gte('created_at', sp.get('from')!);
  if (sp.get('to'))      query = query.lte('created_at', sp.get('to')! + 'T23:59:59');
  if (sp.get('q')) {
    const q = sp.get('q')!;
    query = query.or(`last_name.ilike.%${q}%,first_name.ilike.%${q}%,email.ilike.%${q}%`);
  }

  const { data: participants } = await query;

  const LEVEL_LABEL: Record<string, string> = {
    debutant: 'Débutant', intermediaire: 'Intermédiaire', avance: 'Avancé',
  };

  const headers = ['Nom', 'Prénom', 'Email', 'Service', 'Date de passation', 'Score', 'Niveau', 'Statut'];
  const rows = [headers.map(escapeCsv).join(';')];

  for (const p of participants ?? []) {
    const isComplete = !!p.completed_at;
    rows.push([
      escapeCsv(p.last_name),
      escapeCsv(p.first_name),
      escapeCsv(p.email),
      escapeCsv(p.service),
      escapeCsv(p.completed_at ? new Date(p.completed_at).toLocaleDateString('fr-FR') : ''),
      escapeCsv(isComplete && p.total_score !== null ? `${p.total_score}/${p.max_score}` : ''),
      escapeCsv(isComplete && p.level ? LEVEL_LABEL[p.level] ?? p.level : ''),
      escapeCsv(isComplete ? 'Complet' : 'Incomplet'),
    ].join(';'));
  }

  const csv = '﻿' + rows.join('\r\n'); // BOM UTF-8 pour Excel français
  const date = new Date().toISOString().split('T')[0];

  return new NextResponse(csv, {
    headers: {
      'Content-Type':        'text/csv; charset=utf-8',
      'Content-Disposition': `attachment; filename="participants-saint-joseph-${date}.csv"`,
    },
  });
}
```

- [ ] **Étape 2 — Tester**

Cliquer sur "Export CSV" dans le dashboard admin. Vérifier que le fichier s'ouvre correctement dans Excel (accents lisibles, colonnes bien séparées).

- [ ] **Étape 3 — Commit**

```bash
git add app/api/admin/export/route.ts
git commit -m "feat: export CSV admin — BOM UTF-8, séparateur ;"
```

---

## Task 19 — Seed de démo

**Effort :** S  
**Files :**
- Crée : `scripts/seed-demo-participants.ts`

- [ ] **Étape 1 — Créer `scripts/seed-demo-participants.ts`**

```typescript
if (process.env.NODE_ENV === 'production') throw new Error('Ne pas exécuter en production');

import { createClient } from '@supabase/supabase-js';
import { QUESTIONS } from '../lib/questions';
import { calculateScore, calculateMaxScore, classify, applyKnockout } from '../lib/scoring';
import dotenv from 'dotenv';
dotenv.config({ path: '.env.local' });

const supabase = createClient(
  process.env.NEXT_PUBLIC_SUPABASE_URL!,
  process.env.SUPABASE_SERVICE_ROLE_KEY!
);

type Answers = Record<string, string[]>;

// Profil débutant : Q1=never, Q2=none → knockout
const answersDebutant: Answers = {
  q1: ['never'], q2: ['none'], q3: ['novice'], q4: ['none'], q5: ['none'],
  q6: ['short'], q7: ['never'], q8: [], q9: ['restart'], q10: ['no'],
  q11: ['no'], q12: [], q13: ['no'], q14: ['no'], q15: ['none'],
  q16: ['never'], q17: ['yes_often'], q18: ['no'], q19: ['no'], q20: ['threat'],
};

// Profil intermédiaire
const answersInter: Answers = {
  q1: ['monthly'], q2: ['emails', 'summaries'], q3: ['beginner'], q4: ['chatgpt'], q5: ['auto'],
  q6: ['sentence'], q7: ['sometimes'], q8: ['context'], q9: ['rephrase'], q10: ['heard'],
  q11: ['tried'], q12: ['web_search'], q13: ['no'], q14: ['heard'], q15: ['word'],
  q16: ['sometimes'], q17: ['no'], q18: ['partial'], q19: ['heard'], q20: ['neutral'],
};

// Profil avancé
const answersAvance: Answers = {
  q1: ['daily'], q2: ['emails', 'summaries', 'research', 'meetings'], q3: ['advanced'], q4: ['chatgpt', 'gemini', 'perplexity'], q5: ['external'],
  q6: ['structured'], q7: ['always'], q8: ['context', 'format', 'length', 'examples'], q9: ['iterate'], q10: ['use'],
  q11: ['expert'], q12: ['web_search', 'document_analysis', 'code', 'translation', 'image_gen'], q13: ['regular'], q14: ['expert'], q15: ['word', 'outlook', 'excel'],
  q16: ['always'], q17: ['no'], q18: ['well'], q19: ['yes'], q20: ['opportunity'],
};

async function submitParticipant(
  first_name: string, last_name: string, email: string, service: string, answers: Answers
) {
  const score    = calculateScore(answers, QUESTIONS);
  const maxScore = calculateMaxScore(QUESTIONS);
  const level    = applyKnockout(answers, classify(score, maxScore));

  const p_responses = QUESTIONS.map(q => ({
    question_id:   q.id,
    answer_values: answers[q.id] ?? [],
    points_earned: q.options.filter(o => (answers[q.id] ?? []).includes(o.value)).reduce((s, o) => s + o.points, 0),
  }));

  const { error } = await supabase.rpc('submit_quiz', {
    p_first_name:   first_name,
    p_last_name:    last_name,
    p_email:        email,
    p_service:      service,
    p_completed_at: new Date().toISOString(),
    p_total_score:  score,
    p_max_score:    maxScore,
    p_level:        level,
    p_responses,
  });

  if (error) console.error(`Erreur ${email}:`, error.message);
  else console.log(`✓ ${first_name} ${last_name} → ${level} (${score}/${maxScore})`);
}

async function main() {
  // 5 débutants
  await submitParticipant('Marie', 'Laurent', 'marie.laurent@demo-hpsj.fr', 'RH', answersDebutant);
  await submitParticipant('Pierre', 'Martin', 'pierre.martin@demo-hpsj.fr', 'DSN', { ...answersDebutant, q1: ['tried'], q3: ['beginner'] });
  await submitParticipant('Sophie', 'Bernard', 'sophie.bernard@demo-hpsj.fr', 'Qualité', answersDebutant);
  await submitParticipant('Luc', 'Moreau', 'luc.moreau@demo-hpsj.fr', 'Direction générale', { ...answersDebutant, q17: ['yes_sometimes'] });
  await submitParticipant('Claire', 'Petit', 'claire.petit@demo-hpsj.fr', 'Autre', answersDebutant);

  // 5 intermédiaires
  await submitParticipant('Antoine', 'Durand', 'antoine.durand@demo-hpsj.fr', 'DSN', answersInter);
  await submitParticipant('Julie', 'Leroy', 'julie.leroy@demo-hpsj.fr', 'RH', { ...answersInter, q1: ['daily'], q5: ['internal'] });
  await submitParticipant('Thomas', 'Simon', 'thomas.simon@demo-hpsj.fr', 'Qualité', answersInter);
  await submitParticipant('Emma', 'Michel', 'emma.michel@demo-hpsj.fr', 'Assistantes de direction', answersInter);
  await submitParticipant('Nicolas', 'Lefebvre', 'nicolas.lefebvre@demo-hpsj.fr', 'Service international', { ...answersInter, q6: ['detailed'], q11: ['regular'] });

  // 5 avancés
  await submitParticipant('Sarah', 'Garcia', 'sarah.garcia@demo-hpsj.fr', 'DSN', answersAvance);
  await submitParticipant('Maxime', 'Roux', 'maxime.roux@demo-hpsj.fr', 'Direction générale', answersAvance);
  await submitParticipant('Léa', 'Fournier', 'lea.fournier@demo-hpsj.fr', 'RH', answersAvance);
  await submitParticipant('Hugo', 'Girard', 'hugo.girard@demo-hpsj.fr', 'DSN', answersAvance);
  await submitParticipant('Chloé', 'Morin', 'chloe.morin@demo-hpsj.fr', 'Qualité', answersAvance);

  // 3 incomplets (INSERT direct, bypass RPC)
  const incomplets = [
    { first_name: 'Paul',    last_name: 'Blanc',   email: 'paul.blanc@demo-hpsj.fr',   service: 'DSN' },
    { first_name: 'Isabelle',last_name: 'Dupont',  email: 'isabelle.dupont@demo-hpsj.fr', service: 'RH' },
    { first_name: 'Marc',    last_name: 'Leclerc', email: 'marc.leclerc@demo-hpsj.fr', service: 'Qualité' },
  ];
  for (const p of incomplets) {
    const { error } = await supabase.from('participants').insert(p);
    if (error) console.error(`Erreur incomplet ${p.email}:`, error.message);
    else console.log(`✓ ${p.first_name} ${p.last_name} → incomplet`);
  }

  console.log('\n✅ Seed terminé : 15 complets + 3 incomplets');
}

main();
```

- [ ] **Étape 2 — Installer dotenv**

```bash
npm install -D dotenv tsx
```

- [ ] **Étape 3 — Ajouter le script dans `package.json`**

```json
"seed": "tsx scripts/seed-demo-participants.ts"
```

- [ ] **Étape 4 — Exécuter le seed**

```bash
npm run seed
```

Attendu : 18 lignes `✓ Prénom Nom → level (score/79)`.

- [ ] **Étape 5 — ⚠️ CHECKPOINT HUMAIN #2**

Dans Supabase Studio :
- Vérifier que 15 participants complets et 3 incomplets sont visibles
- Vérifier que la répartition débutant/intermédiaire/avancé est correcte
- Tester le quiz complet en local (/ → /quiz → 20 questions → /quiz/resultat)
- Tester l'admin : login → dashboard → filtres → détail → export CSV
- Valider avant de déployer en production

- [ ] **Étape 6 — Commit**

```bash
git add scripts/seed-demo-participants.ts package.json
git commit -m "feat: seed 15 participants démo + 3 incomplets"
```

---

## Task 20 — Build + README + Déploiement Vercel

**Effort :** S  
**Files :**
- Crée : `README.md`

- [ ] **Étape 1 — Build local**

```bash
npm run build
```

Attendu : build réussi, 0 erreur TypeScript, 0 erreur ESLint. Corriger tout ce qui échoue avant de continuer.

- [ ] **Étape 2 — Créer `README.md`**

```markdown
# Quiz Positionnement IA — HPSJ × Mister IA

Quiz de positionnement Microsoft Copilot Chat pour les professionnels des Hôpitaux Paris Saint-Joseph Marie-Lannelongue.

## Stack

Next.js 15 · TypeScript · Tailwind CSS · Supabase · Vercel

## Prérequis

- Node.js 20+
- Projet Supabase (gratuit suffit)
- Compte Vercel

## Installation locale

```bash
npm install
cp .env.local.example .env.local
# Remplir les 5 variables dans .env.local
npm run dev
```

## Setup Supabase

1. Créer un projet sur supabase.com
2. Copier l'URL et les clés dans `.env.local`
3. Dans SQL Editor, coller et exécuter `supabase/migrations/001_init.sql`
4. Vérifier dans Database → Functions que `submit_quiz` est visible

## Seed de démo

```bash
npm run seed
```

Insère 15 participants fictifs (5 débutants, 5 intermédiaires, 5 avancés) + 3 incomplets.

**Ne jamais exécuter en production** (le script est protégé par une vérification NODE_ENV).

## Déploiement Vercel

1. Pousser le code sur GitHub
2. Connecter le repo sur vercel.com
3. Ajouter les 5 variables d'environnement dans Project Settings → Environment Variables :
   - `NEXT_PUBLIC_SUPABASE_URL`
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY`
   - `SUPABASE_SERVICE_ROLE_KEY`
   - `ADMIN_PASSWORD`
   - `ADMIN_SESSION_SECRET` (générer avec : `openssl rand -base64 32`)
4. Déployer

## Changer le mot de passe admin

Modifier la variable d'environnement `ADMIN_PASSWORD` dans Vercel → redéployer.

## Ajouter ou modifier une question

Éditer `lib/questions.ts`. Les points sont recalculés dynamiquement — pas de constante à mettre à jour.

Pour modifier les seuils de classification, éditer `lib/scoring.ts` → fonction `classify`.

## Remplacer les logos

Remplacer `public/logos/mister-ia.svg` et `public/logos/saint-joseph.svg` par les vrais logos.
```

- [ ] **Étape 3 — Déployer sur Vercel**

```bash
git add README.md
git commit -m "docs: README install + setup + deploy"
git push origin main
```

Puis sur vercel.com :
1. Import Git Repository → sélectionner ce repo
2. Project Settings → Environment Variables → ajouter les 5 variables
3. Deploy

- [ ] **Étape 4 — Vérifier en production**

Tester l'URL Vercel :
- [ ] Landing page visible
- [ ] Quiz complet fonctionnel (/ → /quiz → questions → résultat)
- [ ] Admin accessible sur `/admin/login`
- [ ] Export CSV fonctionne
- [ ] `npm run seed` **NE PAS exécuter en prod** — NODE_ENV=production bloquera l'exécution

- [ ] **Étape 5 — Commit final**

```bash
git commit -m "chore: déploiement production quiz HPSJ"
```

**Critère done :** URL Vercel fonctionnelle, quiz complet testable, dashboard admin accessible.

---

## Récapitulatif

| Tâche | Fichiers principaux | Effort | Statut |
|-------|---------------------|--------|--------|
| T1  | package.json, vitest.config.ts | M | ☐ |
| T2  | 001_init.sql | M | ☐ |
| T3  | lib/questions.ts | M | ☐ |
| T4  | lib/scoring.ts, __tests__/scoring.test.ts | M | ☐ |
| T5  | lib/supabase-server.ts, lib/auth.ts | S | ☐ |
| T6  | middleware.ts | S | ☐ |
| T7  | globals.css, Header.tsx, logos | S | ☐ |
| T8  | app/page.tsx | S | ☐ |
| T9  | api/check-email/route.ts | S | ☐ |
| T10 | QuizForm.tsx, app/quiz/page.tsx | M | ☐ |
| T11 | QuizQuestion.tsx, ProgressBar.tsx | S | ☐ |
| T12 | app/quiz/questions/page.tsx | L | ☐ |
| T13 | api/submit-quiz/route.ts | M | ☐ |
| T14 | ResultCard.tsx, app/quiz/resultat/page.tsx | S | ☐ |
| T15 | admin/login, api/admin/login+logout | S | ☐ |
| T16 | AdminFilters, AdminTableHead, AdminTable, dashboard | L | ☐ |
| T17 | app/admin/participant/[id]/page.tsx | S | ☐ |
| T18 | api/admin/export/route.ts | S | ☐ |
| T19 | scripts/seed-demo-participants.ts | S | ☐ |
| T20 | README.md + deploy | S | ☐ |
